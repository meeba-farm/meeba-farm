// Config settings, and a global state object

var config = {
  w: window.innerWidth - 20,
  h: window.innerHeight - 20,
  wallBuffer: 40,
  bodyBuffer: 10,
  dur: 1000,
  maxSpeed: 150,
  minR: 5,
  maxR: 50,
  color: '#34A853',
  quantity: 15
};

var state = {
  count: 0
};
