// Config settings, and a global state object

var config = {
  w: window.innerWidth - 20,
  h: window.innerHeight - 20,
  buffFactor: 15,
  dur: 1000,
  maxSpeed: 500,
  minR: 5,
  maxR: 35,
  color: '#34A853',
  quantity: 25
};

var state = {
  count: 0
};
