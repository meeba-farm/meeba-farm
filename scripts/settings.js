// Config settings, and a global state object

var config = {
  w: window.innerWidth - 20,
  h: window.innerHeight - 20,
  wallBuffer: 50,
  nodeBuffer: 20,
  dur: 1000,
  maxSpeed: 150,
  minR: 5,
  maxR: 50,
  color: '#34A853',
  quantity: 12
};

var state = {
  count: 0
};
