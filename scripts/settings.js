// Config settings, and a global state object

var config = {
  w: window.innerWidth - 20,
  h: window.innerHeight - 20,
  minR: 20,
  maxR: 20,
  color: '#34A853',
  speed: 250,
  dur: 1000,
  quantity: 10
};

var state = {
  count: 0
};
